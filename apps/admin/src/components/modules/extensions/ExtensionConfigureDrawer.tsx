'use client';

/** @deprecated Prefer ExtensionConfigureModal — kept for existing imports. */
export { ExtensionConfigureModal as ExtensionConfigureDrawer, ExtensionConfigureModal } from './ExtensionConfigureModal';
