;
; VSP Phone v4 — Asterisk logging (ADR-045). Container logs go to stdout for Docker.
;
[general]
dateformat=%F %T.%3q
use_callids=yes

[logfiles]
console => notice,warning,error,verbose(3)
messages => notice,warning,error
