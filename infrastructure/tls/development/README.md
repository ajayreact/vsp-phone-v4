# Development TLS live material

Generated by:

```bash
# Linux/macOS/Git Bash (requires openssl)
./scripts/tls/generate-dev-certs.sh

# Windows PowerShell (.NET, no openssl required)
./scripts/tls/generate-dev-certs.ps1
```

Outputs land in `live/` (gitignored). Never commit keys.
