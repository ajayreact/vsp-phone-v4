# Optional overrides for Phase 4 RTPengine (document / future template).
# Apply via rtpengine.conf or daemon flags after verifying `rtpengine --help`.

# STUN (ICE assist) — TURN remains deferred
# stun-server=stun.l.google.com:19302

# Public/advertise address for NAT labs (Compose: RTPENGINE_ADVERTISE)
# advertised-address=

# Log level passthrough (Compose: RTPENGINE_LOG_LEVEL)
# log-level=6
