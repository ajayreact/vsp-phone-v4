/**
 * Shared constants and types used across VSP Phone v4 applications.
 * Business-specific definitions will be added in later phases.
 */

export const APP_NAME = 'VSP Phone v4';

export type WorkspaceEnvironment = 'development' | 'test' | 'staging' | 'production';
